[package]
name = "pingora-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
pingora = { version = "0.7", features = ["proxy"] }
async-trait = "0.1"
env_logger = "0.11"
log = "0.4"

[lints.rust]
# Pingora's dependency chain uses unsafe internally
unsafe_code = "allow"
